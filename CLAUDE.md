# CoordViz — Claude Code Instructions

## Workflow

After completing any feature request or bug fix, always create a git commit without waiting to be asked. Use a conventional commit message (`feat:`, `fix:`, etc.) and group changes into logical chunks if multiple concerns were touched.
